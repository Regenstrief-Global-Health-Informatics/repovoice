# RepoVoice does not require server env vars for core use.
# GitHub PAT, xAI key, and OpenAI key are entered in the app UI and
# stored only in the browser (localStorage) — never put them here for deploy
# unless you intentionally add a server-side proxy later.

# Optional: Better Auth / database (template defaults)
# DATABASE_URL=
# BETTER_AUTH_SECRET=
